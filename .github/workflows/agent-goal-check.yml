name: Agent — Goal Check
on:
  workflow_dispatch:
    inputs:
      context:
        description: JSON context from dashboard
        required: false
jobs:
  run:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: node scripts/agents/goal-check.js '${{ inputs.context }}'

